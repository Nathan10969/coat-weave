# ---- core deps for coating_kg V1.2.2 ----

# Postgres + pgvector
psycopg2-binary>=2.9
pgvector>=0.2

# Schema + validation
pydantic>=2.0

# Qwen via OpenAI-compatible endpoint (DashScope compatible-mode)
openai>=1.0

# MinerU online API (REST + multipart upload to S3 presigned URLs)
requests>=2.31

# Config + CLI
python-dotenv>=1.0
click>=8.1

# HTTP + retries (used by vlm_describe.py)
httpx>=0.27
tenacity>=8.2

# Tests
pytest>=8.0
